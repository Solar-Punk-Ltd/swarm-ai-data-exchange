# Bee node endpoint
BEE_API_URL=http://localhost:1633

# Catalog feed signer private key (cold key)
BEE_FEED_PK=

# Per-item state feed signer private key (hot key) — MUST differ from BEE_FEED_PK
ITEM_STATE_FEED_PK=

# Postage stamp batch ID for uploads
POSTAGE_BATCH_ID=
