// schema.org vocabulary terms (sc: prefix) used in catalog JSON-LD.
// These are external standards from https://schema.org/ — aliased via the
// catalog @context. Centralized here so the literal strings live in one place.

// JSON-LD @type values
export const SC_IMAGE_OBJECT = 'sc:ImageObject';
export const SC_VIDEO_OBJECT = 'sc:VideoObject';
export const SC_AUDIO_OBJECT = 'sc:AudioObject';
export const SC_TEXT_DIGITAL_DOCUMENT = 'sc:TextDigitalDocument';
export const SC_CREATIVE_WORK = 'sc:CreativeWork';
export const SC_DATASET = 'sc:Dataset';
export const SC_MEDIA_OBJECT = 'sc:MediaObject';
export const SC_DATA_CATALOG = 'sc:DataCatalog';
